spring.application.name=Scrubbr

# Keep uploads sane and in-memory; nothing is written to disk.
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=25MB

server.port=8080
